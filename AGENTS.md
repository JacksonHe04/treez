# Treez 项目执行准则

## 1. 项目使命

Treez 是一个面向个人鉴赏记录的公共评分平台，不只是音乐网站。
它覆盖音乐、影视、书籍和游戏，让用户把自己看过、听过、读过、玩过的作品沉淀为可回顾、可比较、可公开浏览的鉴赏档案。

项目必须实现完整闭环，而不是停留在页面骨架、静态样例或不可用的入口：

1. 作品与创作者数据可被导入、浏览、搜索和关联。
2. 用户通过 iNon SSO 登录。
3. 用户找到或新建条目。
4. 用户提交评分、评论和标签。
5. 评分进入个人档案，同时更新条目的全站聚合结果。
6. 其他人可以公开浏览条目、聚合评分与评论。

最终结果应是一个真实可用、可持续扩展和可部署的产品。

## 2. 已确认的产品边界

首轮覆盖四个领域及以下可独立浏览、评分的实体：

| 领域 | 作品实体 | 创作者实体 |
| --- | --- | --- |
| 音乐 | 专辑、单曲 | 艺术家 |
| 影视 | 影视作品 | 导演 |
| 书籍 | 书籍作品 | 作者 |
| 游戏 | 游戏作品 | 工作室 |

首轮不接第三方内容 API，不建设社区共同编辑、审核或维护机制。
数据来源只有：

- 本地 Notion 导出：`/Users/jackson/iNon/WiKi`，重点关注其 `notion/` 内容。
- 当前连接的云端 Notion 工作区。
- 登录用户主动新增的公共条目。

## 3. 公共性与权限

- 未登录用户可以浏览全部公共条目、聚合评分和评论。
- 写操作必须通过现有 iNon SSO 鉴权。
- 登录用户可以用自己的身份新增条目、评分、评论和添加标签。
- 用户新增的作品或创作者立即成为全站可见、可被其他用户评分的公共条目。
- 本轮所有用户内容强制公开，不提供私密记录。
- 个人主页公开展示该用户的评分与评论历史。
- 权限判断必须在服务端执行，不能只依赖前端隐藏按钮。

## 4. 评分与记录规则

- 统一使用十分制作为底层评分语义。
- 界面同时支持十分制和五星制；五星允许半星，并统一换算到十分制。
- 数据库存储规范化分数，必须能无损保留 Notion 中已有的合法评分精度。
- 同一用户对同一实体只保留一条当前评分；再次提交更新该记录，而不是制造重复评分。
- `ratedAt` 是服务端写入的最近一次评分提交时间，用户不能修改。
- 评论包含 `commentedAt`，默认是创建时刻，但作者可以修改该日期。
- 记录支持标签。
- 本轮不做进度/完成状态、喜欢或收藏功能。
- 聚合评分必须由有效用户评分计算，并展示评分人数；禁止使用静态假数据。

## 5. 核心用户路径

### 未登录用户

1. 进入 Treez，看到产品定位及可公开浏览的内容。
2. 浏览四个领域、条目详情、聚合评分和公开评论。
3. 尝试评分或新增时，被引导至 iNon SSO。
4. 登录完成后返回原目标页面，而不是丢失上下文。

### 已登录用户

1. 进入以个人鉴赏为核心的首页。
2. 查看近期评分、四类鉴赏画像、时间线、个人榜单、标签偏好和快捷新增入口。
3. 搜索作品或创作者并进入详情。
4. 若条目不存在，创建公共条目并补充必要关系。
5. 选择十分制或五星制输入评分，可填写评论、评论日期和标签。
6. 提交后立即看到个人记录、条目聚合评分和公开评论更新。
7. 在个人主页按领域、时间和标签回顾自己的鉴赏历史。

首页首先服务“我看过什么、我如何评价、我的偏好如何变化”，公共热门内容只能作为辅助发现区。

## 6. 最终产品如何呈现

- 首页是一张个人鉴赏杂志：近期记录为主叙事，四类画像、时间线、个人榜单和标签偏好形成辅助版面。
- 领域页是可检索、筛选和排序的公共目录，清楚区分作品类型与创作者类型。
- 详情页同时讲清“它是什么、由谁创作、大家如何评价、我如何评价”，并展示关联实体、聚合分和公开评论。
- 新增与评分采用短而连续的流程；成功后回到详情页并立即呈现本次贡献。
- 个人主页是一份公开鉴赏档案，可按领域、时间和标签理解用户的审美轨迹。

## 7. 页面与信息架构

至少完整实现：

- 个人化首页。
- 四个领域的浏览与筛选页。
- 所有作品及创作者类型的详情页。
- 全局搜索与搜索结果页。
- 新增公共条目流程。
- 评分/评论创建与更新流程。
- 个人公开主页与鉴赏时间线。
- iNon SSO 登录、回跳、会话和退出流程。
- 必要的空状态、错误状态、加载状态和权限状态。

现有 `basic`、`music`、`community`、`rate`、`user` 路由只是历史骨架，可以重构。
不要为了兼容空壳页面而牺牲新的统一信息架构。

## 8. 数据导入与交叉验证

导入不是一次性手工复制，必须实现可重复运行的脚本和可审计流程：

1. 分别盘点本地 Notion 与云端 Notion 的数据库、页面、字段和关系。
2. 建立源字段到 Treez 统一模型的显式映射。
3. 使用标题、别名、创作者、日期和源 ID 等组合规则去重。
4. 对本地与云端记录做交叉验证，不静默覆盖冲突。
5. 保留来源、源 ID、导入批次、校验状态和必要的原始引用。
6. 先生成 dry-run 报告，再写入正式数据库。
7. 导入可幂等重跑，不得制造重复条目、关系、评分或标签。
8. 导入后核对总数、关系完整性、孤立记录、冲突和失败项。

Notion 在本项目中默认是只读来源；没有用户明确授权时，不反向修改云端 Notion。
云端数据必须通过 Notion Plugin 实时读取并先核对 workspace 身份，本地数据直接读取 `/Users/jackson/iNon/WiKi`；两者不能互相替代。
首次导入后，Cloudflare D1 是 Treez 的业务真源，Notion 继续作为带来源标记的导入与校验源。
每次导入设计、字段映射、冲突报告和验收结果写入 `.agents/docs/YYMMDD/`。

## 9. 技术方向

- 应用基线：Next.js App Router、React、TypeScript。
- 组件系统：使用当时最新稳定版 shadcn/ui，不再以 Ant Design 为组件库。
- 数据与后端从零建设在 Cloudflare：
  - D1 存储关系型业务数据。
  - R2 存储需要持久化的封面或导入附件。
  - Workers 提供与 D1/R2 紧密集成的服务端能力。
  - 只有出现明确需求时才引入 KV、Queues 或 Durable Objects。
- 鉴权沿用当前 iNon SSO，不替换为其他身份系统。
- 当前 Next.js 前端可以继续部署在 Vercel；浏览器不直连 D1，公开读走 Worker API，登录写先由服务端验证 iNon 会话，再签名调用 Worker。
- 除非另有经过记录的部署迁移决定，不要破坏现有 `treez.inon.space`、SSO 回调与项目会话。
- Cloudflare 资源通过 Cloudflare Plugin/API 建设和核验；库、框架与 SDK 的最新用法优先通过 Context7 和官方文档确认。
- 实际实现 Cloudflare、Next.js、shadcn/ui 或其他库前，必须检索其当前官方文档和版本指导，不能凭旧记忆编写配置。
- 数据库必须使用迁移管理；本地、预览和生产环境的配置要可复现。
- 密钥只进入环境变量或 Cloudflare Secrets，绝不能写入仓库。

`package.json` 不是技术栈声明。每个依赖都必须在真实产品中有明确用途：

- 现有非 Ant Design 依赖默认是有意引入的，应在真实产品路径中承担明确职责，不能只保留包名。
- 只有确认与目标架构无关并记录理由后才移除依赖；需要替换时一次完成迁移，不能长期保留两套方案。
- Ant Design 组件应在新设计落地时被 shadcn/ui 组件系统完整替换，替换完成后移除相关依赖。
- 优先复用领域模型、表单、评分控件、卡片、筛选器和数据访问层，禁止按四个领域复制四套近似代码。

## 10. 视觉与交互

Treez（树脂）的设计方向是 Heritage：温暖、纸张感、杂志式编排，但不能过度泛黄。

- 品牌主题色：`#708567`，一种浅而克制的 Heritage 鼠尾草绿。
- 通过暖白、浅纸色、鼠尾草绿和温暖近黑建立层级，避免大面积黄褐色。
- 使用编辑式非对称布局、清晰的衬线标题与易读的无衬线正文。
- 以作品封面和视觉内容提供色彩丰富度，界面本身保持安静。
- 少阴影、细分隔线、充足留白；不要做成通用后台仪表盘。
- shadcn/ui 是无障碍与交互基础，必须统一定制为 Treez 视觉语言，不能保留默认示例站观感。
- 同一类实体、评分、标签、筛选、空状态与操作反馈必须在全站一致。
- 桌面端与移动端都要完成真实用户路径，满足键盘操作、焦点可见和 WCAG AA 对比度。

## 11. 强制实施顺序

1. 盘点本地与云端 Notion，输出实体、字段、关系、数量和冲突报告。
2. 基于真实数据确定统一领域模型、唯一性规则、来源模型和 D1 schema。
3. 建设 Cloudflare 资源、迁移、Worker 数据层及本地开发环境。
4. 打通 iNon SSO 到服务端写入边界，验证公开读与登录写。
5. 完成可 dry-run、幂等、可审计的双源导入，再导入真实数据。
6. 建立 shadcn/ui + Treez Heritage tokens 和全站复用组件。
7. 先完成“搜索/新建 → 详情 → 评分 → 聚合 → 个人档案”纵向闭环，再扩展到全部实体。
8. 用真实导入数据完成响应式验收、部署记录与回滚验证。

## 12. 开发与验收原则

- README 中旧的 VoxC、区块链、Vite、Nest.js 或“仅音乐平台”描述均已过时，不得作为实现依据。
- 先理解真实数据，再确定最终 schema；不要先造与 Notion 不匹配的模型。
- 所有按钮、菜单和页面必须有真实行为，不保留无意义占位内容。
- 不用 mock 数据冒充完成；演示数据必须明确标记且不能进入生产导入结果。
- 新功能必须同时覆盖成功、空数据、未登录、无权限和失败路径。
- 验收以完整用户路径和真实数据为主，测试应与风险匹配，不追求形式化堆量。
- 设计、计划、字段映射、迁移说明、导入报告和验收记录统一放在 `.agents/docs/YYMMDD/`。
- 修改前检查现状与官方文档，修改后执行 lint、类型检查、构建及关键路径验证。
- 保持代码、命名、路由、数据模型和交互的一致性；发现重复模式时抽取复用。
- **阶段性自行提交或推送，遵循 `git-commit` Skill，不要堆积大量未暂存或未提交的 Diff**。

## 13. 完成定义

只有同时满足以下条件，Treez 的首轮目标才算完成：

- 四个领域及其作品/创作者实体均可真实浏览、搜索、新增和关联。
- 本地与云端 Notion 数据已交叉验证并可幂等导入 Cloudflare。
- 未登录公开浏览与 iNon SSO 登录写入路径完整可用。
- 评分、评论、日期、标签、聚合统计和个人公开档案形成闭环。
- 个人化首页能真实反映用户的鉴赏历史与偏好。
- UI 已全面切换到统一的 shadcn/ui + Heritage Treez 设计系统。
- 不存在关键占位页、死链、静态假数据或只装未用的核心依赖。
- 生产环境数据迁移、密钥管理、部署和回滚路径已有记录并经过验证。
